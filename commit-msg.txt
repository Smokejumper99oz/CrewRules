feat(admin): rebuild admin dashboard, tenant feature flags, and CSV split-name support

Admin dashboard:
- Replace placeholder with purpose-built tenant admin dashboard
- Attention Required section: unmatched mentees, open requests, missing contacts, staged mentors
- Program Snapshot: 4 stat pills (mentors, mentees, live, unmatched)
- Mentor Activity feed ranked by last milestone completion with tier badges (today/week/month/stale/never)
- Quick Actions grid linking to import, users, mentoring, and requests
- Enterprise Programs locked section hidden by default, toggled via Super Admin

Tenant feature flags:
- Migration 145: tenant_features table with RLS and Super Admin SELECT policy
- Migration 146: backfill RLS policy + seed show_enterprise_programs=false
- lib/tenant-features.ts: getTenantFeatures, getEnabledFeatureSet, setTenantFeature helpers
- Super Admin Tenant Features page with live toggle switches per airline
- Super Admin nav: Feature Flags now links to real page

Admin layout and nav:
- AdminSidebarNav client component with active-page highlighting
- Removed unused Link import from layout

CSV upload: split first/last name support
- detectSplitNameColumns: detects first_name/last_name, First Name/Last Name, mentee_first_name/mentee_last_name (case-insensitive)
- parseFrontierMentoringCsv: synthesizes mentee_full_name from split columns for CSV path
- mentoringAssignXlsxBufferToCsvText: same synthesis for XLSX path
- Existing mentee_full_name files continue to work unchanged

Dashboard weather widget:
- Fix mobile layout: stack vertically on phones, side-by-side on sm+ screens
- Widget goes full-width on mobile instead of overflowing

Misc:
- Founding pilot count uses admin client to bypass RLS
- Admin feedback button added to admin portal layout
