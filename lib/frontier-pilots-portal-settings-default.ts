/**
 * Canonical Settings landing when the old /portal/settings hub is removed.
 * Subpages (e.g. /subscription) remain unchanged; this is only the default entry.
 */
export const FRONTIER_PILOTS_PORTAL_SETTINGS_DEFAULT_PATH =
  "/frontier/pilots/portal/settings/pilot" as const;
